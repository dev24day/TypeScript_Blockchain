{
  "compilerOptions": {
    "module": "CommonJS", //웹에서 구동하면 "AMD", NodeJS에서 구동하면 "commonjs"
    "target": "ES2015", //자바스크립트 버전 설정
    "sourceMap": true //TS의 어느 부분이 JS의 어느 부분인지 => *.js.map으로 구현됨
  },
  "include": [
    //compile에 포함할 파일
    "index.ts"
  ],
  "exclude": ["node_modules"]
}
